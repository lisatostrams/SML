function w = PolCurFit(D_N,M)
M = M+1;
x = D_N(1,:);
t = D_N(2,:);

A = zeros(M,M);
for i = 1:M
    for j = 1:M
        A(i,j) = sum(x.^(i+j-2));
    end
end

T = zeros(M,1);
for i = 1:M
    T(i) = sum(t.*(x.^(i-1)));
end

w = A\T;


end